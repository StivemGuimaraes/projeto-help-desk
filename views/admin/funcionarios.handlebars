<head>
  <title>Listas de funcionarios</title>
</head>
<header
  class="shadow border-secondary border-bottom py-3 mb-4"
  style="
        background: rgb(45, 111, 131);
        background: linear-gradient(
          90deg,
          rgba(45, 111, 131, 1) 17%,
          rgba(28, 46, 76, 1) 100%
        );
      "
>
  <div class="container-fluid">
    <div class="row align-items-center">
      <div class="col-md-2 col-sm-1 col-xl-1">
        <a href="/admin" class="text-decoration-none text-white">
          <img class="img-fluid float-start" src="" alt="logo aqui" />
        </a>
      </div>
      <div class="col-md-3 col-sm-11 col-xl-3">
        <a href="/admin" class="text-white text-decoration-none">
          <h1 class="text-start fs-2">academy help desk</h1>
        </a>
      </div>
    </div>
  </div>
</header>
<main>
  {{> _msg}}
  {{#if error_mensagem}}
  <div class="container mt-5">
    <div class="row justify-content-center text-center">
      <div class="alert alert-danger w-50">{{error_mensagem}}</div>
    </div>
  </div>
{{/if}}
{{#if aviso_mensagem}}
  <div class="container mt-5">
    <div class="row justify-content-center text-center">
      <div class="alert alert-warning w-50">{{aviso_mensagem}}</div>
    </div>
  </div>
{{/if}}
  {{#each funcionario}}
  <div class="container w-50">
  <div class="card mt-4 mb-4">
  <h5 class="card-header">{{this.matricula}}</h5>
  <div class="card-body">
    <h5 class="card-title">{{this.usuario}}</h5>
    <p class="card-text mb-1">Senha: {{this.senha}}</p>
    {{#if telefone_residencial}}
    <p class="card-text mb-1">Telefone celular: {{this.telefone_celular}}</p>
    <p class="card-text">Telefone residencial: {{this.telefone_residencial}}</p>
    {{else}}
    <p class="card-text">Telefone celular: {{this.telefone_celular}}</p>
    {{/if}}   
    <a href="/admin/funcionario/alteracao/{{this.matricula}}" class="btn btn-warning">Alterar</a>
    <a data-bs-toggle="modal" data-bs-target="#exampleModal{{this.matricula}}" class="btn btn-danger">Deletar</a>
  </div>
</div>
</div>
<!--Modal-->
  <div class="modal fade" id="exampleModal{{this.matricula}}" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" style="margin-left: 200px;" id="exampleModalLabel">AVISO</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
        <div class="modal-body text-center">
          <p>Certeza que deseja excluir um funcionário do sistema?</p>
        </div>
        <div class="modal-footer">
          <div style="margin-right: 150px;">
          <button type="button" id="botao_fechar" class="btn btn-secondary" data-bs-dismiss="modal">Fechar</button>
          <a href="/admin/funcionario/exclusao/{{this.matricula}}"><button type="button" class="btn btn-danger">Deletar</button></a>
          </div>
        </div>
      </div>
    </div>
  </div>
  {{/each}}
</main>